{
  "name": "@delete/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python API - no JS build step'",
    "migrate": "uv run alembic upgrade head",
    "migrate:create": "uv run alembic revision --autogenerate -m"
  }
}
